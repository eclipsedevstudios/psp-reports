import styled from 'styled-components';

const BASE = 8;

export const PageFooter = styled.div`
  display: flex;
  justify-content: center;
  margin-top: ${BASE}px;

  > img {
    width: 200px;
  }
`;